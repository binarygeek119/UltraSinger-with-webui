.env
.vscode/
# virtual environment
venv
.venv

# python
**__pycache__

# test data
test_input
test_output

# default output folder
output

# WebUI runtime (local settings; never commit — may contain API keys and paths)
/data/webui_config.json
# Job queue (queue.json) and per-job folders (job.json, logs, audio, etc.)
/data/jobs/
# Download / bulk ZIPs (anywhere in the repo)
*.zip
*.ZIP
/data/temp/
/data/cookies.txt
/data/history/
/UltraSinger*.spec
/registry_path.txt
